window._ = require('lodash');
import Sortable from 'sortablejs';
window.Sortable = Sortable
import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();

// import { createApp } from 'vue'

// const app = createApp({});
// app.component('geo-zone-zone', GeoZoneZone)
//     .mount('#app');
